* match singletons closer
  * `SingI` is better
  * `demote` is wonky now
* write more SingX "proof generator" type classes (e.g. `SingTuple2`)
* how to single operators? what prefix/suffix to use
  * singletons uses `&`? or `%`? idk??
